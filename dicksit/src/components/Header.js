import React from 'react';
import { AppBar, Toolbar } from '@material-ui/core';

const Header = (props) => {
  return (
    <div>
      <AppBar>
        <Toolbar>Dicksit</Toolbar>
      </AppBar>
    </div>
  );
};

export default Header;
